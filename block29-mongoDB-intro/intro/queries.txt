                use bancoDeDados

****** 'db.' referencia o banco do 'use'

db.inventory = dentro da tabela, 'coleção', 'ivenvetory' ('iventory' é uma tabela dentro do banco 'commerce')


******** db.inventory.find();

listar tudo o que tem na tabela desejadda
find vazio é um select all


****** tem que executar os códigos juntos e com ';'

**** insertOne({ 
    objeto javaScript: json
})  

-> o 'id' é criado automaticamente


********** show collections;

mostras as collections, tabelas do banco de dados


***** insertMany([
{

},
{

},
]);

recebe um array

recebe os ids que foram gerados


*********** db.inventory.find({item: 'bike2'});

buscar item especifico
match tem que ser exato. comportamento '==='

lista TODOS OS DOCUMENTOS QUE DA MATCH com a busca


******** db.inventory.find({item: 'bike2', price: 500});

busca item especifico com mais especificação (bike2 AND price como parametros de consulta -> match tem que ser completo)